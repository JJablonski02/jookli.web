{
    "routes": [
        {
            "route": "/",
            "redirect": "/[locale]/(home)",
            "statusCode": 302
        },
        {
            "route": "/:path*",
            "redirect": "/[locale]/(home)",
            "statusCode": 302
        }
    ]
}